version: '3.8'

services:
  postgres:
    container_name: coffee_delivery
    image: postgres
    ports:
      - 5432:5432
    environment:
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=admin
      - POSTGRES_DB=coffee_delivery